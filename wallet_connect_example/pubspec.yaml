name: wallet_connect_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  scan: ^1.4.3
  shared_preferences: ^2.0.7
  flutter_inappwebview: ^5.3.2
  cupertino_icons: ^1.0.2
  wallet_connect:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
