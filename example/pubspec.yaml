name: wallet_connect_example
description: Wallet Connect example app implementing wallet_connect dart library.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  scan: ^1.4.3
  web3dart: ^2.3.0
  shared_preferences: ^2.0.7
  flutter_inappwebview: ^5.7.0
  cupertino_icons: ^1.0.2
  eth_sig_util: ^0.0.3
  wallet_connect:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
